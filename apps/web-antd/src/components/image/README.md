# 使用

```
1: 使用方法：
import usePreviewImg from './usePreviewImage'
const previewImage = usePreviewImg()

2: 调用：

previewImage.show({
   list: ['https://aliyuncdn.antdv.com/vue.png'],
   current: 0
})

show方法接收参数一个对象
list        图片url的数组        如：['xxx', 'xxx]
current     预览第几张图片       默认：0
```
